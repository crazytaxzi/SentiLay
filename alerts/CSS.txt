@import url('https://fonts.googleapis.com/css2?family=Orbitron:wght@600;700;800&family=Oxanium:wght@500;600;700&display=swap');

:root {
  --green: #53ff9a;
  --purple: #b45dff;
  --white: #effff7;
  --muted: #b9d8cb;
  --glass: rgba(6, 12, 17, 0.42);
  --glass-mid: rgba(11, 18, 25, 0.34);
  --glass-edge: rgba(255, 255, 255, 0.11);
}

* {
  box-sizing: border-box;
}

html,
body {
  width: 100%;
  height: 100%;
  margin: 0;
  overflow: hidden;
  background: transparent;
}

body {
  font-family: "Oxanium", "Segoe UI", sans-serif;
  color: var(--white);
}

#alertbox {
  position: absolute;
  inset: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 42px;
  pointer-events: none;
  perspective: 1200px;
  animation: alert-stage-in 460ms cubic-bezier(0.16, 1, 0.3, 1) both;
}

.hellglass-alert {
  position: relative;
  z-index: 4;
  width: min(720px, 92%);
  min-height: 190px;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 32px 48px 36px;
  overflow: hidden;
  border: 1px solid var(--glass-edge);
  border-radius: 22px 5px 22px 5px;
  background:
    linear-gradient(105deg, var(--glass) 0%, var(--glass-mid) 66%, rgba(28, 9, 38, 0.17) 100%);
  box-shadow:
    0 22px 64px rgba(0, 0, 0, 0.48),
    0 0 28px rgba(83, 255, 154, 0.10),
    0 0 42px rgba(180, 93, 255, 0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.08);
  backdrop-filter: blur(16px) saturate(150%);
  -webkit-backdrop-filter: blur(16px) saturate(150%);
  transform-origin: 50% 100%;
  animation: glass-materialize 720ms cubic-bezier(0.16, 1, 0.3, 1) both;
}

.hellglass-alert::before {
  content: "";
  position: absolute;
  inset: 0 auto 0 0;
  width: 5px;
  background: linear-gradient(180deg, var(--green), #8dffbf 42%, var(--purple));
  box-shadow:
    0 0 18px rgba(83, 255, 154, 0.72),
    0 0 30px rgba(180, 93, 255, 0.36);
}

.hellglass-alert::after {
  content: "";
  position: absolute;
  inset: 0;
  background:
    linear-gradient(180deg, rgba(83, 255, 154, 0.03), transparent 30%, transparent 68%, rgba(83, 255, 154, 0.09)),
    linear-gradient(135deg, rgba(255, 255, 255, 0.13), rgba(180, 93, 255, 0.04) 35%, transparent 35%, transparent 70%, rgba(83, 255, 154, 0.04) 70%, transparent 75%);
  mix-blend-mode: screen;
  pointer-events: none;
}

.alert-kicker,
.alert-name,
.alert-amount,
.alert-message {
  position: relative;
  z-index: 3;
  text-align: center;
  overflow-wrap: anywhere;
}

.alert-kicker {
  margin-bottom: 7px;
  color: var(--muted);
  font-family: "Orbitron", "Segoe UI", sans-serif;
  font-size: clamp(16px, 2.1vw, 27px);
  font-weight: 700;
  letter-spacing: 0.12em;
  line-height: 1.25;
  text-transform: uppercase;
  text-shadow: 0 0 12px rgba(83, 255, 154, 0.25);
}

.alert-name {
  max-width: 100%;
  color: var(--white);
  font-family: "Orbitron", "Segoe UI", sans-serif;
  font-size: clamp(34px, 5.8vw, 72px);
  font-weight: 800;
  letter-spacing: 0.025em;
  line-height: 1.02;
  text-shadow:
    0 3px 5px rgba(0, 0, 0, 0.85),
    0 0 18px rgba(83, 255, 154, 0.28),
    0 0 28px rgba(180, 93, 255, 0.16);
}

.alert-amount {
  margin-top: 9px;
  color: var(--green);
  font-size: clamp(22px, 3.4vw, 40px);
  font-weight: 700;
  letter-spacing: 0.05em;
  text-shadow: 0 0 15px rgba(83, 255, 154, 0.48);
}

.alert-message {
  max-width: 92%;
  margin-top: 12px;
  color: var(--white);
  font-size: clamp(17px, 2.2vw, 28px);
  font-weight: 600;
  line-height: 1.3;
  text-shadow: 0 2px 4px rgba(0, 0, 0, 0.88);
}

.scan-sweep {
  position: absolute;
  z-index: 2;
  top: -28%;
  left: -30%;
  width: 28%;
  height: 160%;
  background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.22), rgba(83, 255, 154, 0.16), transparent);
  filter: blur(2px);
  transform: skewX(-14deg);
  animation: scan-pass 1.15s 300ms ease-out both;
}

.holo-field {
  position: absolute;
  z-index: 1;
  left: 10%;
  right: 10%;
  top: 44%;
  bottom: 3%;
  pointer-events: none;
  animation: field-rise 620ms ease-out both;
}

.holo-beam {
  position: absolute;
  left: 6%;
  right: 6%;
  bottom: 14px;
  height: 100%;
  clip-path: polygon(8% 100%, 20% 0, 80% 0, 92% 100%);
  background:
    radial-gradient(90% 90% at 50% 100%, rgba(83, 255, 154, 0.27), rgba(83, 255, 154, 0.06) 42%, transparent 74%),
    linear-gradient(180deg, rgba(180, 93, 255, 0.07), rgba(83, 255, 154, 0.045) 48%, transparent 100%);
  filter: blur(8px);
  opacity: 0.88;
}

.holo-emitter {
  position: absolute;
  left: 16%;
  right: 16%;
  bottom: 0;
  height: 18px;
  border-radius: 999px 999px 0 0;
  background: linear-gradient(90deg, transparent, rgba(83, 255, 154, 0.58) 38%, rgba(180, 93, 255, 0.45) 68%, transparent);
  box-shadow:
    0 0 18px rgba(83, 255, 154, 0.54),
    0 0 36px rgba(180, 93, 255, 0.24);
  filter: blur(1px);
}

#alert-particles {
  position: absolute;
  inset: 0;
  overflow: hidden;
}

#alert-particles .dust {
  position: absolute;
  left: var(--left);
  bottom: -4%;
  width: var(--size);
  height: var(--size);
  border-radius: 50%;
  opacity: 0;
  background: radial-gradient(circle, rgba(239, 255, 247, 0.96), rgba(83, 255, 154, 0.72) 48%, rgba(180, 93, 255, 0.20) 78%, transparent);
  box-shadow: 0 0 9px rgba(83, 255, 154, 0.26);
  animation: dust-rise var(--duration) linear infinite;
  animation-delay: var(--delay);
}

.alert-video {
  position: absolute;
  z-index: 3;
  width: min(330px, 38%);
  max-height: 220px;
  top: 5%;
  left: 50%;
  object-fit: contain;
  transform: translateX(-50%);
  filter: drop-shadow(0 0 22px rgba(83, 255, 154, 0.18));
}

.alert-video.is-hidden,
.alert-kicker.is-empty,
.alert-amount.is-empty,
.alert-message.is-empty {
  display: none;
}

#alertbox.is-leaving {
  animation: alert-stage-out 650ms cubic-bezier(0.55, 0, 1, 0.45) forwards;
}

@keyframes alert-stage-in {
  from { opacity: 0; }
  to { opacity: 1; }
}

@keyframes glass-materialize {
  0% {
    opacity: 0;
    transform: translate3d(0, 44px, -80px) rotateX(-10deg) scale(0.86, 0.48);
    filter: blur(12px);
  }
  45% {
    opacity: 0.86;
    filter: blur(2px);
  }
  100% {
    opacity: 1;
    transform: translate3d(0, 0, 0) rotateX(0) scale(1);
    filter: blur(0);
  }
}

@keyframes field-rise {
  from { opacity: 0; transform: translateY(32px) scaleX(0.72); }
  to { opacity: 1; transform: translateY(0) scaleX(1); }
}

@keyframes scan-pass {
  from { transform: translateX(0) skewX(-14deg); opacity: 0; }
  12% { opacity: 0.9; }
  to { transform: translateX(570%) skewX(-14deg); opacity: 0; }
}

@keyframes dust-rise {
  0% { opacity: 0; transform: translate3d(0, 0, 0) scale(0.7); }
  12% { opacity: 0.42; }
  78% { opacity: 0.24; }
  100% { opacity: 0; transform: translate3d(var(--drift), -130%, 0) scale(1.08); }
}

@keyframes alert-stage-out {
  0% { opacity: 1; transform: translateY(0) scale(1); filter: blur(0); }
  100% { opacity: 0; transform: translateY(-18px) scale(0.96); filter: blur(8px); }
}

@media (prefers-reduced-motion: reduce) {
  #alertbox,
  .hellglass-alert,
  .holo-field,
  .scan-sweep,
  #alert-particles .dust {
    animation-duration: 1ms !important;
  }
}
